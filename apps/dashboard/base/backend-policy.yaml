apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: dashboard-backend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: dashboard-backend
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^build-([0-9]+)$'
    extract: '$1'
