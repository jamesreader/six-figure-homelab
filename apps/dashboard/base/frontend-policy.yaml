apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: dashboard-frontend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: dashboard-frontend
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^build-[0-9]+$'
    extract: '$0'
