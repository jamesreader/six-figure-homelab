apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: dashboard-frontend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: dashboard-frontend
  policy:
    alphabetical:
      order: asc
